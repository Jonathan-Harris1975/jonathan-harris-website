Updated files

1. assets/js/site-ui.js
   - Added support for explicit per-page reveal anchors via [data-jh-header-reveal-anchor].
   - Changed immediate-show logic so shared hero classes no longer force the header visible on page load.
   - Immediate visibility now requires an explicit data-jh-header-show-immediately attribute.

2. privacy-policy/index.html
   - Converted the intro header into the shared reveal anchor.

3. terms-of-use/index.html
   - Converted the intro header into the shared reveal anchor.
   - Updated the inline comment so it no longer claims the page should show the global header immediately.

4. 404.html
   - Added an explicit reveal anchor to the first content section.

5. topics/ai-for-beginners/index.html
   - Added an explicit reveal anchor to the first content section.

6. topics/ai-in-business/index.html
   - Added an explicit reveal anchor to the first content section.

7. topics/ai-in-healthcare/index.html
   - Added an explicit reveal anchor to the first content section.

8. topics/robotics-automation/index.html
   - Added an explicit reveal anchor to the first content section.

Validation
- Ran: python3 scripts/inject_partials.py --validate
- Result: Pages checked 82, In sync 82, Drifted/failed 0

Not changed
- api/docs/index.html was not modified because the repo already contains the canonical hamburger markup and shared JS/CSS wiring.
- No header partial changes were required.
