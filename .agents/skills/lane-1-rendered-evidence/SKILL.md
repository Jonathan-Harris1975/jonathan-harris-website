# Lane 1 rendered evidence skill pack

Use this pack for rendered browser checks. It represents `agent-browser`, `playwright-best-practices`, `webapp-testing` and `browser-use`.

Allowed autonomous outputs: screenshot manifests, viewport coverage, route smoke-test results, interaction traces and flaky-test notes.

Manual gate: do not patch runner architecture or production UI from a flaky result without review.
