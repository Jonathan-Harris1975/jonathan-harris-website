# Lane 1 crawl and source extraction skill pack

Use this pack for discovery and source extraction. It represents `firecrawl-crawl`, `firecrawl-scrape` and `firecrawl-search`.

Allowed autonomous outputs: route inventories, article extraction notes, source discovery ledgers and backlink/research candidate reports.

Manual gate: do not send outreach or make public claims from extracted content without review.
